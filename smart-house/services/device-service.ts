export class DeviceService {
    static changeTemperature(value: number): void {
        // change temperature by device;
    }
    
    static changeLight(value: number): void {
        // change light by device;
    }
}